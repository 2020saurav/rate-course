module.exports = function (req, res) {
    res.send(req.body);
    // Ankita will update this in database.
    // will need switch case here. look at json response and accordingly name the fields and enter in db
}